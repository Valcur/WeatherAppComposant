import XCTest
@testable import WeatherAppComposant

final class WeatherAppComposantTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WeatherAppComposant().text, "Hello, World!")
    }
}
